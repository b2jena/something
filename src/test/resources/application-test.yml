spring:
  cache:
    type: simple
  data:
    redis:
      repositories:
        enabled: false
  redis:
    host: localhost
    port: 6379
    timeout: 2000ms
    lettuce:
      pool:
        enabled: false

logging:
  level:
    com.jena.bookapi: DEBUG